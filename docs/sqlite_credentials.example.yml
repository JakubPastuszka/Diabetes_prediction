# Przykład pod zajęcia — skopiuj sobie lokalnie (tego pliku nie wrzucamy do gita jako credentials).
# Z katalogu suml-projekt/:
#   mkdir -p conf/local
#   cp docs/sqlite_credentials.example.yml conf/local/credentials.yml

db_credentials:
  con: "sqlite:///data/01_raw/dataset.db"
